# Python
__pycache__/
*.pyc
*.egg-info/
.venv/

# Testing
.pytest_cache/
.coverage
coverage.json
junit.xml
htmlcov/

# Local runtime data
*.db

# Local Claude Code settings
.claude/settings.local.json
.claude/worktrees/

# macOS
.DS_Store

# Design handoffs (local reference only, not version-controlled)
android/design/

# Local account-setup/credential-testing scripts, not part of the application
src/AccountSetup/

# VSCode workspace file (machine-local, not shared project config)
*.code-workspace

# Security audit reports (may contain vulnerability detail; local reference only)
AuditReports/

# Locally-fetched Claude Code skill payloads - skills-lock.json pins the
# exact source/version; these are reinstalled from it on demand, not
# vendored (same treatment as .venv/ above).
.agents/
.claude/skills/
