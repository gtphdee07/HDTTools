<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Camera-overlay spike only (ClaudePlans/2026-08-27-android-camera-overlay-spike.md);
         production capture still uses the system camera intent and needs no permission. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:name=".RigCheckApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RigCheck">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RigCheck"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Isolated research spike (ClaudePlans/2026-08-27-android-camera-overlay-spike.md) -
             not part of production navigation; RigCheckNavHost/ChooserScreen are untouched.
             Launch directly for manual verification, e.g.:
             adb shell am start -n com.rigcheck.app/.ui.experiments.cameraoverlay.CameraOverlaySpikeActivity -->
        <activity
            android:name=".ui.experiments.cameraoverlay.CameraOverlaySpikeActivity"
            android:exported="true"
            android:label="Camera Overlay Spike"
            android:theme="@style/Theme.RigCheck" />
    </application>

</manifest>